%==========================================================
\section{Signaux inter-threads et détection de deadlocks}
%==========================================================

% ── SLIDE 1 : POSIX signals — single address-space column ──────────────
% cw=4.2  kb=4.8  ch=7.8  all hardcoded to avoid TikZ macro issues
\begin{frame}{Signaux POSIX : passages mode utilisateur $\leftrightarrow$ noyau}
\centering
\begin{tikzpicture}[scale=0.86]

  % ── backgrounds : utilisateur en haut, noyau en bas ─────────────────
  \fill[blue!10]   (0, 3.0) rectangle (4.2, 7.8);   % user (top)
  \fill[orange!14] (0, 0.0) rectangle (4.2, 3.0);   % kernel (bottom)

  % highlighted kernel row — sigprocmask only
  \fill[orange!40] (0, 1.2) rectangle (4.2, 1.8);

  % highlighted user rows
  \fill[gray!22]   (0, 3.0) rectangle (4.2, 3.6);   % pile
  \fill[green!28]  (0, 4.8) rectangle (4.2, 5.4);   % alarm_handler
  \fill[blue!28]   (0, 6.0) rectangle (4.2, 6.6);   % PC

  % ── grid ────────────────────────────────────────────────────────────
  \draw[thick] (0, 0) rectangle (4.2, 7.8);
  \foreach \y in {0.6,1.2,1.8,2.4,3.0,3.6,4.2,4.8,5.4,6.0,6.6,7.2}
    \draw[gray!40, thin] (0,\y) -- (4.2,\y);

  % ── boundary ────────────────────────────────────────────────────────
  \draw[red!65, very thick, dashed] (-0.2, 3.0) -- (7.9, 3.0);

  % ── kernel labels ───────────────────────────────────────────────────
  \node[font=\scriptsize] at (2.1, 1.50) {\texttt{sigprocmask()}};
  \node[font=\tiny, gray] at (2.1, 0.90) {$\vdots$};
  \node[font=\tiny, gray] at (2.1, 2.55) {$\vdots$};

  % ── user labels ─────────────────────────────────────────────────────
  \node[font=\scriptsize]          at (2.1, 3.30) {Pile (stack)};
  \node[font=\tiny, gray]          at (2.1, 4.20) {$\vdots$};
  \node[font=\scriptsize]          at (2.1, 5.10) {\texttt{alarm\_handler()}};
  \node[font=\tiny, gray]          at (2.1, 5.70) {$\vdots$};
  \node[font=\scriptsize\bfseries] at (2.1, 6.30) {Code interrompu (PC)};
  \node[font=\tiny, gray]          at (2.1, 7.20) {$\vdots$};

  % ── section labels (rotated) ─────────────────────────────────────────
  \node[font=\small\bfseries, blue!60,         rotate=90] at (-0.65, 5.40) {Utilisateur};
  \node[font=\small\bfseries, orange!70!black, rotate=90] at (-0.65, 1.50) {Noyau};

  % ── column title ─────────────────────────────────────────────────────
  \node[font=\small\bfseries] at (2.1, 8.3) {Espace d'adressage virtuel};

  % ── EXECUTION ZIGZAG ────────────────────────────────────────────────
  % 3 arrows, well-spaced at x = 5.2, 6.0, 6.8
  % ① timer : noyau → alarm_handler  [blue, UP]
  \draw[->, >=Stealth, line width=1.3pt, blue!65] (5.2, 1.5) -- (5.2, 5.1);
  \node[blue!65, font=\tiny, anchor=west] at (5.3, 4.0) {\cn{1} timer};

  % ② sigreturn : alarm_handler → noyau  [red, DOWN]
  \draw[->, >=Stealth, line width=1.3pt, red!70] (6.8, 5.1) -- (6.8, 1.5);
  \node[red!70, font=\tiny, anchor=west] at (6.9, 2.0) {\cn{2} sigreturn};

  % ③ reprise : noyau → PC  [blue, UP]
  \draw[->, >=Stealth, line width=1.3pt, blue!65] (8.8, 1.5) -- (8.8, 6.3);
  \node[blue!65, font=\tiny, anchor=west] at (8.9, 4.8) {\cn{3} reprise};

  % horizontal connectors
  \draw[gray!45, thin] (5.2, 5.1) -- (6.8, 5.1);   % handler : ①→②
  \draw[gray!45, thin] (6.8, 1.5) -- (8.8, 1.5);   % noyau   : ②→③

  \node[font=\footnotesize\bfseries] at (6.0, 8.3) {Exécution};

\end{tikzpicture}
\end{frame}

% ── SLIDE 2 : Signal mask — POSIX vs uint32_t ──────────────────────────
\begin{frame}{Masque de signaux : POSIX vs notre implémentation}
\centering
\begin{tikzpicture}[scale=0.78]

  % ── POSIX side (left) ───────────────────────────────────────────────
  \node[font=\small\bfseries, orange!70!black] at (-4.2, 3.2) {Masque POSIX (\texttt{sigset\_t})};

  % 32-bit integer visual (8 groups of 4 bits)
  \foreach \i in {0,...,7} {
    \fill[orange!20] ({-6.8 + \i*0.72}, 2.4) rectangle ({-6.8 + \i*0.72 + 0.66}, 2.9);
    \draw[orange!50] ({-6.8 + \i*0.72}, 2.4) rectangle ({-6.8 + \i*0.72 + 0.66}, 2.9);
    \node[font=\tiny] at ({-6.8 + \i*0.72 + 0.33}, 2.65) {\texttt{xxxx}};
  }
  % bit 0 and bit 31 labels
  \node[font=\tiny, gray] at (-6.8+0.33,  2.25) {0};
  \node[font=\tiny, gray] at (-6.8+7*0.72+0.33, 2.25) {31};
  \node[font=\tiny]       at (-4.2, 2.05) {\texttt{sigset\_t} = entier 32 bits (POSIX)};

  % sigprocmask box
  \node[draw, fill=red!15, rounded corners=3pt,
        minimum width=3.5cm, minimum height=0.65cm, font=\scriptsize]
    (spm) at (-4.2, 1.2) {\texttt{sigprocmask()} - syscall};
  \node[font=\tiny, gray, align=center] at (-4.2, 0.32)
    {modifie le masque\\$\Rightarrow$ 1 appel noyau à chaque usage};

  % stored in kernel
  \node[draw, fill=orange!20, rounded corners=3pt,
        minimum width=3.5cm, minimum height=0.55cm, font=\scriptsize]
    at (-4.2, -0.45) {stocké dans \texttt{task\_struct} (noyau)};

  % ── separator ───────────────────────────────────────────────────────
  \draw[gray!50, thick, dashed] (0, -0.85) -- (0, 3.5);
  \node[font=\footnotesize\itshape, gray] at (0, -1.1) {vs};

  % ── OUR side (right) ────────────────────────────────────────────────
  \node[font=\small\bfseries, blue!65] at (4.2, 3.2) {Notre implémentation (\texttt{uint32\_t})};

  % 32-bit integer visual (8 groups of 4 bits)
  \foreach \i in {0,...,7} {
    \fill[blue!20] ({1.6 + \i*0.72}, 2.4) rectangle ({1.6 + \i*0.72 + 0.66}, 2.9);
    \draw[blue!40]  ({1.6 + \i*0.72}, 2.4) rectangle ({1.6 + \i*0.72 + 0.66}, 2.9);
    \node[font=\tiny] at ({1.6 + \i*0.72 + 0.33}, 2.65) {\texttt{xxxx}};
  }
  \node[font=\tiny, gray] at (1.6+0.33,       2.25) {0};
  \node[font=\tiny, gray] at (1.6+7*0.72+0.33,2.25) {31};
  \node[font=\tiny]       at (4.2, 2.05) {\texttt{uint32\_t pending\_signals}};

  % no syscall
  \node[draw, fill=green!18, rounded corners=3pt,
        minimum width=3.5cm, minimum height=0.65cm, font=\scriptsize]
    at (4.2, 1.2) {opération bit à bit - 0 syscall};
  \node[font=\tiny, gray, align=center] at (4.2, 0.32)
    {modifié avec \texttt{|=} et \texttt{\&=}\\entièrement en espace utilisateur};

  % stored in thread_s
  \node[draw, fill=blue!15, rounded corners=3pt,
        minimum width=3.5cm, minimum height=0.55cm, font=\scriptsize]
    at (4.2, -0.45) {stocké dans \texttt{thread\_s} (user)};

  % ── Why uint32_t and not int ────────────────────────────────────────
  \node[draw, fill=gray!10, rounded corners=4pt,
        minimum width=8.5cm, minimum height=1.1cm,
        font=\scriptsize, align=center]
    at (0, -2.65)
    {\texttt{uint32\_t} et non \texttt{int} : taille de \texttt{int} variable \\selon l'architecture
     (ex: x86 32-bit, x86\_64 64-bit) \\ \texttt{uint32\_t} garantit \textbf{exactement} 32 bits, non signé};

\end{tikzpicture}
\end{frame}

% --- User-level signals ---
\begin{frame}{Signaux inter-threads en espace utilisateur}
\centering
\begin{tikzpicture}[
  th/.style={draw, rounded corners=3pt, fill=blue!20,
             minimum width=2.8cm, minimum height=2.8cm, font=\footnotesize, align=center},
  field/.style={draw, fill=gray!10, minimum width=2.6cm,
                minimum height=0.5cm, font=\scriptsize},
  arr/.style={->, >=Stealth, thick},
]
  % Thread A
  \node[th] (ta) at (-3.5, 0) {};
  \node[font=\small\bfseries] at (-3.5, 1.1) {Thread A};
  \node[font=\scriptsize, align=center] at (-3.5, 0.2)
    {\texttt{thread\_kill(B, sig)}\\$\Downarrow$\\ met le bit \texttt{sig} de\\ \texttt{pending\_signals} à 1};

  % Thread B
  \node[th] (tb) at (3.5, 0) {};
  \node[font=\small\bfseries] at (3.5, 1.1) {Thread B};
  \node[field] at (3.5,  0.35) {\texttt{pending\_signals}};
  \node[field] at (3.5, -0.20) {\texttt{sig\_handlers[32]}};
  \node[field] at (3.5, -0.75) {\texttt{sigwait\_mask}};

  \draw[arr, red!70] (ta.east) -- (tb.west)
    node[midway, above, font=\scriptsize] {\texttt{thread\_kill}};

  \node[draw, fill=orange!20, rounded corners=3pt,
        minimum width=3.5cm, minimum height=0.7cm, font=\footnotesize, align=center]
    (del) at (0, -2.2)
    {\texttt{deliver\_pending\_signals()}\\appelé dès que ce thread prend la main};

  \draw[arr, blue!60] (tb.south) |- (del.east);

  \node[draw, fill=green!15, rounded corners=3pt,
        font=\scriptsize, align=center] (sw) at (3.5, -4.3)
    {\texttt{thread\_sigwait(set, \&signo)} : se met BLOCKED\\dans \texttt{blockedQueue}
           jusqu'à ce qu'un signal\\de \texttt{set} arrive (\texttt{thread\_kill} l'envoie)
           \\\texttt{*signo} $\leftarrow$ numéro du signal reçu};

  \draw[arr, green!60!black] (tb.south) -- (sw.north);
\end{tikzpicture}
\end{frame}

% --- Deadlock detection ---
\begin{frame}{Détection de DEADLOCK}
\centering
\begin{tikzpicture}[
  th/.style={draw, rounded corners=2pt, minimum width=1.2cm,
             minimum height=0.6cm, font=\footnotesize\bfseries},
  link/.style={<->, >=Stealth, thin, gray!60},
  qlbl/.style={font=\scriptsize\bfseries, anchor=east},
]
  % ── AVANT (gauche) ───────────────────────────────────────────────────
  \node[font=\small\bfseries] at (2.2, 2.3) {État courant};

  % readyQueue : th0 th1
  \node[qlbl, blue!70]          at (0.0, 1.5) {readyQueue};
  \node[th, fill=blue!30] (ra) at (0.7, 1.5) {th0};
  \node[th, fill=blue!20] (rb) at (2.1, 1.5) {th1};
  \draw[link] (ra.east) -- (rb.west);

  % blockedQueue : th2
  \node[qlbl, orange!80!black]   at (0.0, 0.5) {blockedQueue};
  \node[th, fill=orange!30] (ba) at (0.7, 0.5) {th2};

  % ── FLÈCHE TRANSITION ────────────────────────────────────────────────
  \draw[->, >=Stealth, very thick, red!60] (3.3, 1.0) -- (4.7, 1.0)
    node[midway, above, font=\tiny, red!70] {th0, th1 se bloquent};

  % ── APRÈS (droite) ───────────────────────────────────────────────────
  \node[font=\small\bfseries, red!70] at (8.5, 2.3) {DEADLOCK};

  % readyQueue : vide
  \node[qlbl, blue!70]            at (5.0, 1.5) {readyQueue};
  \node[font=\normalsize, gray]   at (5.9, 1.5) {$\emptyset$};

  % blockedQueue : th0 th1 th2
  \node[qlbl, orange!80!black]    at (5.0, 0.5) {blockedQueue};
  \node[th, fill=orange!30] (da) at (5.7, 0.5) {th1};
  \node[th, fill=orange!25] (db) at (7.1, 0.5) {th0};
  \node[th, fill=orange!20] (dc) at (8.5, 0.5) {th2};
  \draw[link] (da.east) -- (db.west);
  \draw[link] (db.east) -- (dc.west);

  % EDEADLK
  \node[draw, fill=red!25, rounded corners=3pt,
        minimum width=3.5cm, minimum height=0.6cm, font=\small\bfseries]
    (dead) at (5.7, -0.8) {\texttt{return EDEADLK}};
  \draw[->, >=Stealth, thick, red!70] (da.south) -- (dead.north);

  \node[font=\scriptsize, gray, align=center] at (5.7, -1.65)
    {détecté dans \texttt{thread\_join} :\\
     \texttt{readyQueue} $= \emptyset$ avant de se bloquer $\Rightarrow$ personne ne peut nous réveiller};

\end{tikzpicture}
\end{frame}
